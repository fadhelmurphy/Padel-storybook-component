# Storybook

My setup storybook (only storybook SPA)

## Getting Started

run the development server:

```bash
npm run local
# or
yarn local
```

How to build:

```bash
npm run build
# or
yarn build
```